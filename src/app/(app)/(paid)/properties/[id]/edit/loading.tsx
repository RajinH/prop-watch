import PageSkeleton from '@/components/ui/PageSkeleton'

// Overrides the properties grid skeleton for the wizard pages.
export default function Loading() {
  return <PageSkeleton variant="form" />
}
